# VidSetu Environment Configuration

# Google OAuth 2.0 Client ID (Web Application)
# Get yours from: https://console.cloud.google.com/apis/credentials
# Authorized JavaScript Origins:
#   - http://localhost:5173
#   - https://your-app-name.netlify.app
VITE_GOOGLE_CLIENT_ID=560981530750-iv02m9n04o8b579sj1nha6vjdaccv5rl.apps.googleusercontent.com

# Central Server Library Folder ID (The Google Drive Folder ID of your VidSetu_Videos folder)
# Share your VidSetu_Videos folder as "Anyone with the link can view" in Google Drive.
# Then copy the Folder ID from the Drive URL: https://drive.google.com/drive/folders/<FOLDER_ID>
VITE_CENTRAL_FOLDER_ID=1UHhSa7VjHL0d0C8J5Iw-mSK4xvPUEc4C

# Library Videos Folder Name
VITE_DEFAULT_FOLDER_NAME=VidSetu_Videos

# Uploads Target Folder Name
VITE_UPLOADS_FOLDER_NAME=VidSetu_Uploads

# --- Server-side only: set this in the Netlify dashboard, never in a client .env file ---
# Powers two things:
#  - Deleting a temporary share from Drive right after a recipient downloads it (they have no
#    Google session of their own, so this can't be done from their browser).
#  - Proxying every download through our own domain (/api/download-file) instead of a
#    drive.google.com link, so mobile browsers don't get intercepted into a Google account-picker
#    prompt (drive.google.com is a verified Android App Link for the Drive app).
#
# Setup:
#  1. In Google Cloud Console, create a Service Account (APIs & Services > Credentials).
#  2. Enable the Google Drive API for that project.
#  3. Create a JSON key for the service account and copy its contents as a single line.
#  4. Share BOTH folders in Drive with the service account's email
#     (looks like name@project.iam.gserviceaccount.com):
#       - VidSetu_Uploads as Editor (needed to delete consumed one-time shares)
#       - VidSetu_Videos (your permanent library folder) as Viewer (needed to proxy downloads)
#  5. In Netlify: Site configuration > Environment variables > add GOOGLE_SERVICE_ACCOUNT_KEY
#     with the full JSON key content as the value.
# GOOGLE_SERVICE_ACCOUNT_KEY={"type":"service_account","client_email":"...","private_key":"..."}


